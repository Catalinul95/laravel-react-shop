export default interface IProduct {
    id?: number,
    title: string,
    slug: string,
    image: string;
    description: string;
    price: number,
    status: string,
    created_at: string,
    updated_at: string,
}
